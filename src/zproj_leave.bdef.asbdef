projection;
strict ( 2 );
use draft;

define behavior for ZProj_leave //alias <alias_name>
{
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
//  use action Prepare;
use action Resume;

  use create;
  use update;
  use delete;
}